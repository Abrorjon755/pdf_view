class Constants {
  const Constants._();

  static const String theme = "settings.theme";
  static const String locale = "settings.locale";

  static const String idCount = "storage.id";
  static const String products = "storage.products";
}
